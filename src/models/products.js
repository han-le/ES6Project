class Product {
  constructor(id_, tenSP_, gia_, hinhanh_) {
    this.id = id_;
    this.tenSP = tenSP_;
    this.gia = gia_;
    this.hinhAnh = hinhanh_;
  }
}

export default Product; //Only applies to file have 1 item
